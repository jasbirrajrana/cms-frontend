query getPost($slug: String!) {
  getPostByslug(slug: $slug) {
    _id
    slug
    createdAt
    updatedAt
    body
    featureImage
    title
    description
    author {
      _id
      username
      email
    }
  }
}
