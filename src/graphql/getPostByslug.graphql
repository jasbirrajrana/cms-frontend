query getPostByslug($slug: String!) {
  getPostByslug(slug: $slug) {
    _id
    slug
    createdAt
    updatedAt
    body
    featureImage
    likes
    title
    description
    author {
      _id
      username
      email
    }
  }
}
